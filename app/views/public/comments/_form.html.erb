<%= form_with(model:[post, comment],url: post_comments_path(post),method: :post,remote: true) do |f| %>
  <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに" %>
  <%= f.submit "送信する" %>
<% end %>